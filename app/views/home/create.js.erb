// js code that animates modal for adding counter attacks
// allows modal pop up, and dismisses it with close button

// example code from twitter bootstrap - modified by me
$(".usersTable").replaceWith("<%=j render :partial=> 'users/update_user', :locals => {users: @users }%>");
$("input[type=text]").val("");

$('#myModal').modal('hide');
$('body').removeClass('modal-open');
$('.modal-backdrop').remove();
